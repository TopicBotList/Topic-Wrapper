exports.domain = "https://topiclist.xyz"
exports.endpoints = {
    getBots: "/api/bots/:id/info", //gets the stats from api.topiclist.xyz
    postStats: "/api/bots/:id" //posts stats using api.topiclist.xyz //gets the user
}